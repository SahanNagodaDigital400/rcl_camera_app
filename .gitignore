# Agent tooling
.bmad-loop/runs/
.bmad-loop/archive/
.bmad-loop/cache/
.bmad-loop/policy.toml
_bmad/render/

# Dev TLS for the Vite dev server: a self-signed cert generated per machine by
# `make certs`. Never committed -- a private key in git is a private key
# published, and every checkout sharing one key is worse than no TLS at all.
apps/web/certs/

# Proof of concept (standalone, own venv and data)
poc/Tiles/
poc/Tiles-prepared/
poc/index/
poc/models/
poc/queries/
poc/certs/
poc/.venv/

# The ONNX backbone: 346 MB of weights, fetched once per machine by
# `make model` and never committed. `shared/vision` looks for it here by
# default and honours ROCELL_MODEL_PATH when it lives somewhere else.
shared/vision/shared_vision/models/

# Python
__pycache__/
.pytest_cache/
.ruff_cache/
.venv/
*.egg-info/

# Node / front end
node_modules/
dist/
coverage/

# Secrets — AGENTS.md Policy: never commit or hardcode a secret, test fixtures
# included. Environment or a secret store only.
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx

# Junk
.DS_Store
*.log

# Per-developer Claude Code settings (machine-specific: LAN dev origin, etc.)
.claude/settings.local.json
