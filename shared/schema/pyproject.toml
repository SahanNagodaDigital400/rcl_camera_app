[project]
name = "shared-schema"
version = "0.1.0"
description = "Shared types/contracts between apps/api and scripts/ingest"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["schema*"]
