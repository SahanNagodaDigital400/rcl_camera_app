[project]
name = "shared-vision"
version = "0.1.0"
description = "Preprocessing + embedding pipeline, called identically by apps/api and scripts/ingest"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["vision*"]
